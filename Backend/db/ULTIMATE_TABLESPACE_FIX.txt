# 🔥 ULTIMATE MYSQL TABLESPACE FIX #1813 - 5 MINUTE SOLUTION

## STEP 1: STOP XAMPP MYSQL
1. XAMPP Control Panel → Stop MySQL
2. Wait 10 seconds

## STEP 2: DELETE CORRUPTED FILE
```
Delete this exact file:
C:\xampp\mysql\data\genconnect\users.ibd
```
(If folder hidden: View → Show → Hidden items)

## STEP 3: RESTART MYSQL
XAMPP Control Panel → Start MySQL

## STEP 4: CREATE TABLE (phpMyAdmin → genconnect → SQL)
```sql
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    fullName VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    number VARCHAR(20) NOT NULL,
    dateOfBirth DATE NOT NULL,
    gender ENUM('male', 'female', 'other') NOT NULL,
    password VARCHAR(255) NOT NULL,
    coverPhoto VARCHAR(500),
    avatar VARCHAR(500),
    posts INT DEFAULT 0,
    followers INT DEFAULT 0,
    following INT DEFAULT 0,
    createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## STEP 5: TEST
```
npm start
```
Registration works! ✅

---

**ALTERNATIVE:** Recreate database:
phpMyAdmin → Databases → Drop `genconnect` → Create `genconnect` → Run full schema

**YOU ARE 5 MINUTES FROM WORKING BACKEND! 🚀**
